import SwiftUI

enum DSFont {
    static let sectionLabel = Font.caption.weight(.semibold)
    static let primaryAmount = Font.title2.weight(.semibold).monospacedDigit()
    static let secondaryAmount = Font.body.weight(.medium).monospacedDigit()
    static let monoCaption = Font.caption.monospacedDigit()
    static let cardTitle = Font.subheadline.weight(.semibold)
    static let cardBody = Font.subheadline
}
